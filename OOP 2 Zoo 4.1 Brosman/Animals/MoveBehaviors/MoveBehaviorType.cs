namespace Animals
{
    /// <summary>
    /// The enum to represent an animal's move behavior type.
    /// </summary>
    public enum MoveBehaviorType
    {
        Fly,
        Pace,
        Swim,
        Climb,
        Hover,
        NoMove
    }
}