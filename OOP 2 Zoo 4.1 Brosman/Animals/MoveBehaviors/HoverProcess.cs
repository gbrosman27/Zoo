namespace Animals.MoveBehaviors
{
    /// <summary>
    /// The enum used to represent the hover process.
    /// </summary>
    public enum HoverProcess
    {
        Hovering,
        Zooming
    }
}